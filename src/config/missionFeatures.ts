/**
 * Presentation-layer toggles for mission gameplay (family portal + kids routes).
 */
export const ENABLE_CINEMATIC_MISSION_MODE = true;

/** Dev-only: show grade diagnostics overlay in cinematic mission mode */
export const ENABLE_CINEMATIC_GRADE_DIAGNOSTICS = false;
