/**
 * Placeholder child route so React Router matches panel URLs under /family-hub.
 * FamilyHubLayout renders the actual panel from location.pathname.
 */
export default function FamilyHubPanelRoute() {
  return null;
}
