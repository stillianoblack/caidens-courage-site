import React, { useEffect, useState } from 'react';
import { MILESTONE_CELEBRATION_EVENT, type MilestoneCelebration as Celebration } from '../../lib/achievementEvents';
export default function MilestoneCelebration(){const [item,setItem]=useState<Celebration|null>(null);useEffect(()=>{const show=(event:Event)=>{setItem((event as CustomEvent<Celebration>).detail);window.setTimeout(()=>setItem(null),4500);};window.addEventListener(MILESTONE_CELEBRATION_EVENT,show);return()=>window.removeEventListener(MILESTONE_CELEBRATION_EVENT,show);},[]);if(!item)return null;return <aside className="milestoneCelebration" role="status" aria-live="polite"><strong>{item.title}</strong><span>Your progress is saved.</span><button type="button" onClick={()=>setItem(null)} aria-label="Dismiss celebration">×</button></aside>}
